// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  typescript: {
    ignoreBuildErrors: true, // ⛔ WARNING: Use only if you're sure!
  },
}

module.exports = nextConfig
